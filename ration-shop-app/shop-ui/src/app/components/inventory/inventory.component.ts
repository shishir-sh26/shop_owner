import { Component } from '@angular/core';

@Component({
  selector: 'app-inventory',
  standalone: true,
  templateUrl: './inventory.component.html', // Make sure this matches your filename
  styleUrl: './inventory.component.scss'
})
export class InventoryComponent { } // This "export" name must exist